<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example">
    <application>
        <provider android:authorities="${applicationId}.provider.init"
                  android:name=".InitProvider"
                  android:exported="false"/>
    </application>
</manifest>